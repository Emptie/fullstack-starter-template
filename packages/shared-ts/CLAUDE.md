# Shared TypeScript

自动生成的 TypeScript 类型。**所有文件由 make generate 自动生成，不要手动编辑。**

Source of truth 是 packages/shared-py/src/starter_shared/types/。
修改类型 → 改 shared-py → make generate。直接改这个目录的文件会被下次 generate 覆盖。
