[project]
name = "admin-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared-py",
]

[tool.uv.sources]
shared-py = { workspace = true }

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
