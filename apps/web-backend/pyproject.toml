[project]
name = "web-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared-py",
    "alembic",
]

[tool.uv.sources]
shared-py = { workspace = true }

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
