# Docker deployment configuration.
# Used by docker-compose.yml — do NOT use for local development.
# Local dev uses the root .env file instead.

DB_HOST=postgres
DB_PORT=5432
DB_USER=root
DB_PASSWORD=123456
DB_NAME=starter
DB_TABLE_PREFIX=
